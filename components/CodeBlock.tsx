import { View } from "react-native";
import CodeBlocksWrapper from "../classes/CodeBlocksWrapper";

interface Props {
    wrapper: CodeBlocksWrapper;
}

const CodeBlock = (props: Props) => <View></View>;

export default CodeBlock;

